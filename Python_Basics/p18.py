#WAP to input ASCII value and print corresponding character.

ch = int(input("Enter ASCII value:\t"))
i = chr(ch)     # chr(number) : : This function converts number to its corresponding ASCII character.
print("Corresponding character to ASCII value is:\t", i)
